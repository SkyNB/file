<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:jee="http://www.springframework.org/schema/jee"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
    http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd
    http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee-4.0.xsd
    http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd
    http://www.springframework.org/schema/tx  http://www.springframework.org/schema/tx/spring-tx-4.0.xsd
    http://www.springframework.org/schema/aop  http://www.springframework.org/schema/aop/spring-aop-4.0.xsd
    http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-4.0.xsd
    http://www.springframework.org/schema/rabbit
    http://www.springframework.org/schema/rabbit/spring-rabbit-2.0.xsd">

    <!-- rabbitMQ配置 -->
     <rabbit:connection-factory
            id="rabbitConnectionFactoryApp"
            host="${rabbitMQ.host}"
            username="${rabbitMQ.username}"
            password="${rabbitMQ.password}"
            port="${rabbitMQ.port}"/>

    <rabbit:admin id="connectAdminApp" connection-factory="rabbitConnectionFactoryApp"/>

    <!-- autoDelete:是否自动删除 durable:持久化  -->
    <rabbit:queue name="sis.application.new-order" durable="true" auto-delete="false" declared-by="connectAdminApp" />

    <!-- topic主题 -->
    <rabbit:topic-exchange name="sis.order.topic.exchange" xmlns="http://www.springframework.org/schema/rabbit" durable="true" auto-delete="false" declared-by="connectAdminApp">
        <bindings>
            <binding queue="sis.application.new-order" pattern="sis.application.new-order" />
        </bindings>
    </rabbit:topic-exchange>

    <!-- 创建rabbitTemplate 消息模板类 -->
    <bean id="rabbitTemplate" class="org.springframework.amqp.rabbit.core.RabbitTemplate">
        <constructor-arg ref="rabbitConnectionFactoryApp"></constructor-arg>
    </bean>



    <!-- autoDelete:是否自动删除 durable:持久化  -->
    <rabbit:queue name="sis.order.confirm" durable="true" auto-delete="false" declared-by="connectAdminApp" />

    <!-- topic主题 -->
    <rabbit:topic-exchange name="sis.order.topic.exchange" xmlns="http://www.springframework.org/schema/rabbit" durable="true" auto-delete="false" declared-by="connectAdminApp">
        <bindings>
            <binding queue="sis.order.confirm" pattern="sis.order.confirm" />
        </bindings>
    </rabbit:topic-exchange>

    <!-- 创建rabbitListener 消息监听对象 -->
    <bean id="queueListenerApp" class="com.tasfe.sis.application.service.impls.SyncOrderServiceImpl">
    </bean>


    <bean id="listenerContainer"
          class="org.springframework.amqp.rabbit.listener.DirectMessageListenerContainer">
        <property name="queueNames" value="sis.order.confirm"></property>
        <property name="connectionFactory" ref="rabbitConnectionFactoryApp"></property>
        <property name="channelAwareMessageListener" ref="queueListenerApp"></property>
        <property name="acknowledgeMode" value="MANUAL"></property>
    </bean>
</beans>